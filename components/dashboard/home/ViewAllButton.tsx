"use client";
import { Button } from "@/components/ui/Button";

const ViewAllButton = () => {
  return (
    <Button
      variant="ghost"
      label="View All"
      className="max-w-[90px] px-3 font-medium max-h-9 mt-0!"
    />
  );
};

export default ViewAllButton;
